[build]
  command = "npm run build"
  publish = "dist"

# Functionsの場所をNetlifyに改めて教え込む
[functions]
  directory = "netlify/functions"

# 404エラーを防ぐための魔法の設定
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
